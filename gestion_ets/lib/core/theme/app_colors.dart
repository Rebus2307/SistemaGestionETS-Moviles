import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Estados (aplican igual en ambos modos)
  static const Color success = Color(0xFF2E8B57);
  static const Color error = Color(0xFFC0392B);
  static const Color warning = Color(0xFFD4A017);
}
